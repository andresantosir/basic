const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // a primeira parte da expressão é a comparação, caso seja verdadeiro aparecerá Aprovado, caso contrário, Reprovado.

console.log(resultado(7.14))
console.log(resultado(5.2))
